#pragma once

#include <vector>

/**
 * @brief 比较两个 int 类型的 vector
 *
 * @param vec1
 * @param vec2
 * @return true     两个 vector 相等
 * @return false    两个 vector 不相等
 */
bool CompareIntVectors(std::vector<int> &vec1, std::vector<int> &vec2);
